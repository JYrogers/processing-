class Ball {
  
  
float x,y, radius, vx, vy, ax, ay;
int fill_colors;

public Ball(flat, x , float y, float radius, float vx, float vy, float ax, float ay, int fill_color) {
  this.x = x;
  this.y = y;
  this.radius = radius;
  this.vx = vx;
  this.vy = vy;
  this.fill_color = fill_color

}
